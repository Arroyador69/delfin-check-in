'use client';

/**
 * Dashboard en /dashboard para que el middleware valide la cookie (igual que /reservations)
 * y el contenido se muestre sin depender de la ruta /.
 */
export { default } from '../page';
